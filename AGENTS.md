# AI 代理協作指南

歡迎！為了確保我們之間協作順暢，請您在開始任何任務前，仔細閱讀並遵守以下準則。

> [!IMPORTANT]
> **⚠️ 請保持根目錄整潔**：
> *   **核心程式碼**：必須存放在 `src/` 目錄下。
> *   **數據資料**：必須存放在 `data/` 目錄下。
> *   **產出檔案**：所有自動產生的結果、日誌、封存檔，**必須** 統一存放在 `output/` 目錄下（例如 `output/results/`, `output/logs/`）。
> *   **工具腳本**：維護與自動化相關的腳本應存放在 `scripts/` 目錄下。
> *   **嚴禁**將程式碼檔案或產出物直接散落在根目錄（除了 `main.py`、設定檔以及特定的外部資料夾如 `lo2cin4bt-main`）。

## 溝通語言

*   **全程使用繁體中文**：所有的溝通、程式碼註解、提交訊息、以及產出的文件，都必須使用繁體中文。
*   **檔案命名規範**：所有檔案名稱必須使用英文，避免中文或特殊字元，以確保跨平台相容性。
*   **內容編碼**：所有文字檔案必須使用UTF-8編碼，確保繁體中文正確顯示。

## 工作流程

## 📋 格式示範 4：使用箭頭 + 對比
### 模型選擇策略
```
階段 1：開發測試
   → 推薦模型：gemma3:4b
   → 顯存需求：~4GB
   → 速度：快 ⚡
   → 推理能力：中等 ⭐⭐⭐

階段 2：正式回測
   → 推薦模型：deepseek-r1:7b
   → 顯存需求：~7GB
   → 速度：中等 ⚡⚡
   → 推理能力：強 ⭐⭐⭐⭐

階段 3：深度分析
   → 推薦模型：qwen3:14b
   → 顯存需求：~14GB
   → 速度：慢 ⚡⚡⚡
   → 推理能力：很強 ⭐⭐⭐⭐⭐
```
---
## 📋 格式示範 2：使用 Emoji + 引用區塊
### Colab 免費版 vs 付費版比較
> 🆓 **免費版**
> ├─ 使用 14B+ 模型：❌ 不支援
> ├─ 連續運行 24 小時：❌ 不支援
> ├─ 高速 GPU (V100/A100)：❌ 不支援
> └─ 處理 10 年以上數據：⚠️ 速度慢
>
> > 💎 **Colab Pro ($9.99/月)**
> > ├─ 使用 14B+ 模型：✅ 支援
> > ├─ 連續運行 24 小時：✅ 支援
> > ├─ 高速 GPU (V100/A100)：✅ 支援
> > └─ 處理 10 年以上數據：✅ 速度快

1.  **理解與規劃**：在收到任務後，請先充分探索與分析，並提出一份詳細的執行計畫。
2.  **使用者批准**：**絕對禁止**在沒有獲得使用者明確批准之前，對任何檔案進行修改。計畫必須先獲得同意。
3.  **執行與驗證**：在獲得批准後，依照計畫執行。每一步完成後，都應進行驗證，確保結果符合預期。

## 檔案命名規範

*   **英文檔案名稱**：所有檔案名稱必須使用英文，避免使用中文、空格或特殊字元。
*   **目錄結構**：檔案應按照功能分類存放，避免散落。
*   **版本控制**：對於程式碼檔案，使用英文描述性名稱，不包含版本號在檔案名稱中。

### 程式碼檔案命名範例
```
✅ 正確：integrated_strategy.py, taifex_data_downloader.py
❌ 錯誤：整合策略.py, 台期貨下載器.py
```

### 目錄檔案命名範例
```
✅ 正確：data/taifex_official.db, scripts/update_data.py
❌ 錯誤：資料/台期貨.db, 腳本/更新資料.py
```

## 開發日誌維護

*   **使用結構化日誌系統**：所有開發日誌都應記錄在 `docs/logs/` 目錄中，每個日誌都是獨立的 Markdown 檔案。
*   **檔名格式**：`YYYY-MM-DD_HH-MM_brief-description.md`（例如：`2025-11-22_20-15_v9.1_structure_optimization.md`）
*   **目錄結構**：日誌按月份分類存放於 `docs/logs/YYYY-MM/` 目錄下。
*   **建立新日誌**：
    *   **推薦方式**：使用 `python utils/create_log.py` 自動產生日誌檔案
    *   **手動方式**：參考 `docs/logs/README.md` 中的格式規範
*   **時區標準**：記錄時間戳記時，必須使用「亞洲/台北」時區，並包含時區資訊（例如 `CST`）。
*   **內容規範**：每個日誌應包含標題、日期、作者、摘要、主要變更、驗證結果等章節。
*   **語言規範**：日誌內容必須使用繁體中文，確保團隊溝通一致性。

## 錯誤避免指南

*   **編碼問題預防**：所有文字檔案必須儲存為UTF-8編碼，避免編碼錯誤導致中文顯示異常。
*   **檔案路徑處理**：使用 `pathlib.Path` 或 `os.path.join()` 處理檔案路徑，避免硬編碼路徑字串。
*   **跨平台相容**：避免使用Windows特定的路徑分隔符，確保在不同作業系統上都能正常運行。
*   **依賴管理**：在程式碼中加入適當的錯誤處理，避免因網路或外部服務中斷導致程式崩潰。

### 常見錯誤與解決方案

#### 編碼錯誤
```python
# ✅ 正確：指定UTF-8編碼
with open('file.txt', 'w', encoding='utf-8') as f:
    f.write('繁體中文內容')

# ❌ 錯誤：未指定編碼
with open('file.txt', 'w') as f:
    f.write('繁體中文內容')
```

#### 檔案路徑錯誤
```python
# ✅ 正確：使用pathlib或os.path
from pathlib import Path
data_dir = Path('data')
file_path = data_dir / 'taifex_official.db'

# ❌ 錯誤：硬編碼路徑
file_path = 'data\\taifex_official.db'  # Windows限定
```

#### 中文檔案名稱
```python
# ✅ 正確：英文檔案名稱
'taifex_official_downloader.py'

# ❌ 錯誤：中文檔案名稱
'台期貨官方下載器.py'
```

## 專案結構總覽

```
sp_lab_v10/
├── src/                    # 核心程式碼
│   ├── integrated_strategy.py
│   └── taifex_data_integration.py
├── scripts/               # 工具腳本
│   ├── taifex_official_downloader.py
│   ├── build_taifex_official_db.py
│   └── taifex_official_update.py
├── data/                  # 資料檔案
│   ├── taifex.db
│   ├── taifex_options.db
│   └── taifex_official.db
├── docs/                  # 文件
│   └── logs/             # 開發日誌
├── output/               # 產出檔案
└── AGENTS.md             # 本指南檔案
```

感謝您的合作！

## 📚 歷史紀錄與日誌查詢

為了讓其他 AI 工具或團隊成員能快速查詢過往的工作紀錄，我們將所有開發、執行與分析的日誌統一放在 `docs/logs/` 目錄下。每次執行任務或產生重要輸出時，請在此目錄新增一個 Markdown 日誌檔案，遵循以下命名規則：

```
YYYY-MM-DD_HH-MM_brief-description.md
```

日誌內容應包含：
- 任務名稱與目標
- 執行步驟與指令
- 產生的檔案或結果摘要
- 任何錯誤或需注意的事項

其他 AI 工具只要讀取 `docs/logs/` 內的檔案，即可取得完整的歷史脈絡，避免重複工作。

> [!NOTE]
> 若需要在程式中自動寫入日誌，可使用 `utils/create_log.py` 產生符合規範的檔案。
